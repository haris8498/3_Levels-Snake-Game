namespace Snake_Game_Project
{
    public enum GameMode
    {
        Easy,      // No boundaries, no AI snakes, standard speed
        Normal,    // Boundary obstacles, AI snakes present, moderate speed
        Difficult  // Boundary obstacles + extra obstacles, AI snakes present, high speed
    }
}
